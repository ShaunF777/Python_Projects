s = input("s: ")
t = input("t: ")

if s == t:
    print("same")
else:
    print("different")
# in c they were diff, because char * represented the addresses of different vars
# in py it interprets that were comparing the inside values automatically

